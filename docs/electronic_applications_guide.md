# 📋 Система Электронных Заявок

Автоматическая обработка вебхук-заявок на вступление/восстановление в Вооруженные Силы с отправкой информации в личные сообщения пользователей.

## 🚀 Быстрый Старт

### 1️⃣ Настройка через `/settings`

```
/settings → Выберите "Электронные заявки"
```

**Доступные настройки:**
- **📂 Установить канал** - канал, в который летят вебхук-заявки
- **✅ Успешная реакция** - эмодзи при успешной доставке сообщения
- **❌ Реакция ошибки** - эмодзи при ошибке (пользователь не найден / DM закрыты)
- **🔍 Регулярка для парсинга** - паттерн для извлечения Discord-тега из сообщения
- **📊 Статус** - показывает текущую конфигурацию

### 2️⃣ Редактирование Шаблонов

```
/message_request_edit <файл.md> [application_type]
```

**Параметры:**
- `template_file` (обязательный) - файл .md с новым шаблоном
- `application_type` (опциональный) - тип заявки: `вступление` (по умолчанию) или `восстановление`

**Пример:**
```
/message_request_edit мой_шаблон.md вступление
```

**Права:** Только администраторы бота или пользователи с ролью "Администратор"

---

## ⚙️ Как это работает

### Автоматическая обработка вебхук-сообщений

1. **Получение заявки** - вебхук отправляет сообщение в настроенный канал
2. **Парсинг Discord-тега** - бот извлекает Discord-тег из сообщения по регулярке
3. **Поиск пользователя** - бот ищет пользователя на текущем сервере
4. **Отправка сообщения** - бот отправляет заявку в личные сообщения
5. **Реакция** - бот ставит реакцию:
   - ✅ успешная доставка
   - ❌ ошибка (пользователь не найден / DM закрыты)

### Форматы Discord-тегов

Система поддерживает несколько форматов благодаря **двухуровневой системе парсинга**:

**Уровень 1: Основной regex из конфига** (работает с предсказуемыми форматами)
- `Дискорд для связи с вами: @k1maru.exe`
- `Дискорд для связи с вами: inter#1674`
- `Discord для связи с вами: username`
- `discord для связи с вами: @username` (case-insensitive)

**Уровень 2: Fallback (если основной не сработал)**
- Бот ищет любые потенциальные Discord-теги в форме `@username` или `username`
- Автоматически фильтрует служебные слова (для, вас, мск, кпп)
- Берёт первый найденный валидный тег

**Примеры из вебхука:**
```
Дискорд для связи с вами:(Пример- @k1maru.exe)
Дискорд для связи с вами:(Пример- inter#1674)
```

**Регулярка по умолчанию:**
```
(?:Дискорд|discord|Discord)[^:]*:\s*(?:\([^)]*\))?\s*@?(\w[\w.#\d-]*)
```

**Как работает трёхуровневая система парсинга:**

1. **Уровень 1: Основной regex** (включает флаг `DOTALL` для работы с переносами строк)
   - Ищет "Дискорд:" или "Discord:" (case-insensitive)
   - Опционально пропускает скобки типа "(Пример- @user)"
   - Извлекает тег, работает даже если тег на следующей строке

2. **Уровень 2: Теги с @ (более надежное совпадение)**
   - Если основной regex не сработал, ищем `@username`
   - Берет первый найденный тег с @ (обычно это нужный пользователь)

3. **Уровень 3: Fallback для простых форматов**
   - Ищет любые потенциальные теги
   - **Автоматически исключает служебные слова**: 
     - для, вас, мск, кпп, пример, user, пользователь, имя, фамилия, возраст, номер, паспорта, онлайн, часы, служить, военный
   - Исключает чисто цифровые последовательности
   - Требует минимум 3 символа в теге

Вы можете настроить основную регулярку в `/settings` → Электронные заявки → 🔍 Регулярка для парсинга. 
Если она не сработает, система всё равно найдёт тег через fallback-паттерны.

---

## 📁 Структура Файлов

### Основной код:
- **`cogs/electronic_applications.py`** - Event listener для вебхук-сообщений
- **`cogs/electronic_applications_commands.py`** - Команда `/message_request_edit`
- **`forms/settings/electronic_applications.py`** - UI для настроек в `/settings`

### Данные:
- **`data/electronic_applications.md`** - Шаблон для вступления (редактируется через команду)
- **`data/electronic_applications_restore.md`** - Шаблон для восстановления (редактируется через команду)
- **`data/config.json`** - Конфигурация (настраивается через `/settings`)

---

## 🎯 Логирование

Все события логируются с префиксом `ELEC_APP`:

```
[INFO] ELEC_APP: Получено вебхук-сообщение в канале 1452359439502803097
[INFO] ELEC_APP: Найден Discord-тег: k1maru.exe
[INFO] ELEC_APP: Пользователь найден по имени: k1maru
[INFO] ELEC_APP SUCCESS: Сообщение отправлено пользователю k1maru
[INFO] ELEC_APP: Реакция добавлена: ✅
```

При ошибках:
```
[WARNING] ELEC_APP: Пользователь не найден на сервере: unknown_user
[WARNING] ELEC_APP: ДМ закрыты для пользователя username
[ERROR] ELEC_APP: Ошибка при добавлении реакции: ...
```

---

## 🔧 Конфиг (data/config.json)

```json
{
    "electronic_applications": {
        "enabled": true,
        "channel_id": 1452359439502803097,
        "template_path": "data/electronic_applications.md",
        "discord_tag_pattern": "(?:Дискорд|discord|Discord)[^:]*:\\s*(?:\\([^)]*\\))?\\s*@?(\\w[\\w.#\\d-]*)",
        "success_reaction": "✅",
        "failure_reaction": "❌",
        "templates": {
            "вступление": {
                "path": "data/electronic_applications.md",
                "type_keyword": "Заявление на вступление"
            },
            "восстановление": {
                "path": "data/electronic_applications_restore.md",
                "type_keyword": "Заявление на восстановление"
            }
        }
    }
}
```

---

## 📝 Формат Шаблонов (.md)

Используется обычный Markdown:

```markdown
# Заголовок первого уровня
## Заголовок второго уровня

Обычный текст описания.

- Пункт списка 1
- Пункт списка 2
- Пункт списка 3

Ещё какой-то текст.
```

**Особенности:**
- `#` заголовки остаются как есть (Discord конвертирует в крупный текст)
- `-` списки остаются как есть
- Обычный текст отправляется как есть
- Эмодзи 🚀 сохраняются

---

## ⚠️ Частые Проблемы

### ❌ "Не сработала реакция"
**Причина:** Бот не имеет прав на реакции в канале.
**Решение:** Выдайте боту право `Add Reactions` на канал электронных заявок.

### ❌ "Пользователь не найден, хотя он на сервере"
**Возможные причины:**
1. Пользователь ввел неправильный Discord-ник
2. Это редко, но возможно: формат ника отличается от ожиданий

**Как это работает:**
1. Бот сначала пытается найти пользователя по основной регулярке из конфига
2. Если не сработала, использует fallback-парсинг (ищет любой `@username` или `username` в сообщении)
3. Если и тот не сработал — выводит реакцию ❌ и логирует `WARN: Пользователь не найден`

**Решение:** 
- Проверьте логи в канале с префиксом `ELEC_APP WARN`
- Убедитесь, что пользователь действительно на сервере
- Попробуйте поменять регулярку в `/settings` если нужны особые форматы

### ❌ "ДМ не доходят"
**Причина:** У пользователя закрыты личные сообщения от серверных ботов.
**Решение:** Попросите пользователя открыть DM в настройках приватности.

---

## 🚀 Примеры Использования

### Пример 1: Первая настройка

```
1. /settings → Электронные заявки
2. Нажимаем "📂 Установить канал"
3. Вводим #вебхуки (или ID канала)
4. Система готова!
```

### Пример 2: Обновление шаблона

```
1. Редактируем файл electronic_applications.md локально
2. /message_request_edit (прикладываем файл) вступление
3. Шаблон обновлён ✅
```

### Пример 3: Поменять реакции на дикие эмодзи

```
1. /settings → Электронные заявки
2. "✅ Успешная реакция" → вводим 🎉
3. "❌ Реакция ошибки" → вводим 💔
4. Теперь система ставит 🎉 и 💔 вместо ✅ и ❌
```

---

## 🎓 Техническая архитектура

### Поток обработки

```
Discord Webhook Message
    ↓
on_message Event Listener
    ↓
Parse Discord Tag (Regex)
    ↓
Find User on Guild
    ↓
Load Template (Markdown)
    ↓
Convert to Discord Format
    ↓
Send DM to User
    ↓
Add Reaction (✅ / ❌)
    ↓
Log Event
```

### Компоненты

| Компонент | Файл | Роль |
|-----------|------|------|
| Event Listener | `electronic_applications.py` | Обработка вебхук-сообщений |
| Commands | `electronic_applications_commands.py` | Редактирование шаблонов |
| Settings UI | `electronic_applications.py` | Конфигурация через `/settings` |
| Config Manager | `config_manager.py` | Сохранение конфига |

